id: monitoring.trigger_watcher
display_name: Trigger Watcher
description: Watches for specific patterns or events that require attention
inputs:
  - user_message
  - conversation_history
  - stance
outputs:
  - observation
  - claims
  - stance_delta
cost_estimate: 400
cooldown: 30
priority: always_on
safety_profile: read_only
prompt_template: trigger_watcher.md
version: 1
